{
  "name": "hardhat-project",
  "scripts": {
    "deploy:mantle": "npx hardhat run scripts/deploy.ts --network mantle-testnet",
    "test": "npx hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.14.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.3",
    "dotenv": "^16.0.3"
  }
}
